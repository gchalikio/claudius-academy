.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
.cursor/
node_modules/
.env
.env.*

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Local-only decks live under presentations/local/ together with their
# own registry file (presentations/local/decks.js). The whole folder is
# gitignored so the public repo never sees personal decks or their names.
presentations/local/

# Local Claude Code editor settings (skills/ is committed, settings.local.json is not)
.claude/settings.local.json
.claude/settings.json
